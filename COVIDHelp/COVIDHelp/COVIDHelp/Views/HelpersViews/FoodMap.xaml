<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
              xmlns:maps="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
             x:Class="COVIDHelp.Views.HelpersViews.FoodMap">
    <ContentPage.Content>
        <Grid>
            <maps:Map x:Name="map"
                     InitialCameraUpdate="18.4801972,-69.942111"
                  ItemsSource="{Binding PlaceNearbys, Mode=TwoWay}">
                <maps:Map.ItemTemplate>
                    <DataTemplate>
                        <maps:Pin Position="{Binding Geometry.Location.Position}"
                              Icon="{Binding Icon,Mode=TwoWay}" 
                               Label="{Binding Name}"
                                />
                    </DataTemplate>
                </maps:Map.ItemTemplate>
            </maps:Map>
        </Grid>
    </ContentPage.Content>
</ContentPage>