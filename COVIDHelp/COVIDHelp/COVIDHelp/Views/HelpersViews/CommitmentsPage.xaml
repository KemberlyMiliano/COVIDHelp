<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="COVIDHelp.Views.HelpersViews.CommitmentsPage"
             NavigationPage.HasNavigationBar="False">
    <StackLayout Margin="20">
        <Label Text="Historial" FontSize="30" FontFamily="{StaticResource BoldFont}" Margin="0,10,0,20" TextColor="Black"/>
        <CollectionView ItemsSource="{Binding Historial}"  
                            Grid.Row="2"
                            SelectedItem="{Binding SelectedCommitment}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10,10,0,10">
                        <Frame BackgroundColor="#0272A6" Grid.RowSpan="2" CornerRadius="5" Padding="1">
                            <Grid Padding="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackLayout  Grid.Column="1">
                                    <Label   
                                           Text="{Binding NeededPerson}"    
                                           FontFamily="{StaticResource BoldFont}"
                                        Margin="0,10,0,0"/>
                                    <Label
                                           Text="{Binding Status}"    
                                           FontFamily="{StaticResource NormalFont}" />
                                </StackLayout>
                                <Image Source="{Binding Image}" 
                                           HeightRequest="40"
                                       Margin="0,0,15,0"/>
                            </Grid>
                        </Frame>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>